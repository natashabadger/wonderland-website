function setup(){
  cnv = createCanvas(window.innerWidth, window.innerHeight);
    cnv.parent('wrapperDiv');
    cnv.position(20, 20);
    background(65, 135, 163);
}

function draw(){
  fill(200);
ellipse(100, 2, 122);
}
